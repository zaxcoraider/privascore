import React from "react";
import ReactDOM from "react-dom/client";
import PrivaScoreApp from "./PrivaScoreApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PrivaScoreApp />
  </React.StrictMode>
);
